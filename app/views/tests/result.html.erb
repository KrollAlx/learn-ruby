<div class="row">
  <div class="col"></div>
  <div class="col-6">
    <h2 class="text-center mt-4">
      Вы правильно ответили на <%= @right_answers_count %> из <%= @count_questions %> вопросов
    </h2>

    <% @questions.each_with_index do |question, index| %>
      <h3 class="mt-3">Вопрос №<%= index + 1 %>.<br> <%= question.text %></h3>
      <% question.answers.each_with_index do |answer, i| %>
        <p>
          <%= i + 1 %>) <%= answer.text %>
        </p>
      <% end %>
      <p>
        Ваш ответ: <%= @results[index][:answer] %>, <%= @results[index][:result] %>.
      </p>
    <% end %>

    <%= link_to "Список доступных тестов", tests_path, class: "btn btn-danger text-black mt-4 mb-4" %>
  </div>
  <div class="col"></div>
</div>